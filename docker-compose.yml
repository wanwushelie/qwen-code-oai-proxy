services:
  qwen-proxy:
    build: .
    ports:
      - "${PORT:-8006}:${PORT:-8006}"
    volumes:
      - ~/.qwen:/root/.qwen
      - ~/.local/share/qwen-proxy:/root/.local/share/qwen-proxy
    environment:
      - NODE_ENV=production
      - PORT=${PORT:-8006}
      - HOST=${HOST:-0.0.0.0}
      - DEBUG_LOG=${DEBUG_LOG:-false}
      - LOG_FILE_LIMIT=${LOG_FILE_LIMIT:-20}
      - STREAM=${STREAM:-false}
      - DEFAULT_ACCOUNT=${DEFAULT_ACCOUNT:-}
      - QWEN_CODE_AUTH_USE=${QWEN_CODE_AUTH_USE:-true}
      - API_KEY=${API_KEY:-}

    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "const req = require('http').request({ hostname: 'localhost', port: '${PORT:-8006}', path: '/health', method: 'GET', headers: { 'X-API-Key': '${API_KEY:-}' } }, (res) => { process.exit(res.statusCode === 200 ? 0 : 1) }); req.on('error', () => process.exit(1)); req.end();",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
